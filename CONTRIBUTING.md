# Contributing to PAFFO

Thank you for your interest in contributing to PAFFO.

## How to Contribute

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/my-feature`)
3. Commit your changes (`git commit -m 'Add my feature'`)
4. Push to the branch (`git push origin feature/my-feature`)
5. Open a Pull Request

## Development

This is a static HTML/CSS/JS site. To run locally:

```bash
# Using Python
python3 -m http.server 8000

# Using Node.js
npx serve .
```

Then open `http://localhost:8000` in your browser.

## Guidelines

- Keep changes focused and minimal
- Test across browsers (Chrome, Firefox, Safari)
- Ensure responsive design works on mobile
- Follow existing code style (no linter configured)

## Questions?

Open an issue or contact **hello@paffo.ru**.
